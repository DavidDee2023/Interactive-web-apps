firstName = 'John';
age = 35;
hobby = 'Coding';

const logTwice = () => {
  console(parameter)
  console(parameter)
};

function hobby () {
  logTwice(`Hello, ${firstname} (${age}). I love ${hobby}!`)
};

hobby()